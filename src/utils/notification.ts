import { NotificationContainer, NotificationManager } from 'react-notifications'

const NotiContainer = NotificationContainer

const NotiManager = NotificationManager

export { NotiContainer, NotiManager }
